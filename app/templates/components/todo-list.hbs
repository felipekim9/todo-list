<div class="todo-app">
  {{todo-input createItem=createItem}}
  <ul class="todo-list">
    {{#each model as |item|}}
      {{todo-item item=item editItem=editItem deleteItem=deleteItem}}
    {{/each}}
  </ul>
  {{info-label model=model}}
</div>
